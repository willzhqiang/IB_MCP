# ============================================
# IB MCP Server 环境变量配置
# ============================================
# 复制此文件为 .env 并修改相应的值
# cp env.example .env

# ============================================
# Gateway 配置
# ============================================
# Gateway 监听端口（必须与 conf.yaml 中的 listenPort 一致）
GATEWAY_PORT=5055

# Gateway API 端点路径
GATEWAY_ENDPOINT=/v1/api

# Gateway 内部 URL（容器间通信使用）
GATEWAY_INTERNAL_BASE_URL=https://api_gateway

# Gateway 外部 URL（从主机访问使用）
GATEWAY_BASE_URL=https://localhost

# ============================================
# MCP Server 配置
# ============================================
# MCP Server 监听地址
MCP_SERVER_HOST=0.0.0.0

# MCP Server 端口
MCP_SERVER_PORT=5002

# MCP Server 路径
MCP_SERVER_PATH=/mcp

# MCP 传输协议（streamable-http 或 stdio）
MCP_TRANSPORT_PROTOCOL=streamable-http

# MCP Server 外部 URL（从主机访问使用）
MCP_SERVER_BASE_URL=http://localhost

# MCP Server 内部 URL（容器间通信使用）
MCP_SERVER_INTERNAL_BASE_URL=http://mcp_server

# ============================================
# Tickler 服务配置（会话保活）
# ============================================
# Tickler 目标 URL
TICKLE_BASE_URL=https://localhost:5055

# Tickler 端点路径
TICKLE_ENDPOINT=/v1/api/tickle

# Tickler 调用间隔（秒）
TICKLE_INTERVAL=60

# ============================================
# 模块过滤配置（可选）
# ============================================
# 只包含指定的模块（逗号分隔，不包含空格）
# 可用模块: Alerts, Contract, Events Contracts, FA Allocation Management,
#          FYIs & Notifications, Market Data, Options Chains, Order Monitoring,
#          Orders, Portfolio, Scanner, Session, Watchlists
# INCLUDED_TAGS=Portfolio,Session,Market Data

# 排除指定的模块（逗号分隔，不包含空格）
# EXCLUDED_TAGS=FA Allocation Management

# ============================================
# 日志配置（可选）
# ============================================
# MCP_SERVER_LOG_LEVEL=DEBUG

